<div class="container">
  <h1 class="my-3"> 記事投稿 </h1>

  <form method='post' action='/article_post' enctype='multipart/form-data'>
  <div class="form-group">
    <input class="form-control" type="text" name="title" value="" placeholder="タイトル">
  </div>
  <div class="form-group">
    <textarea class ="form-control" type="text" name="body" cols="50" rows="16" placeholder="記事を入力"></textarea>
  </div>
  </form>

  <br>

    <p class="mt-3">・カテゴリーを選択</p>
 <div class="mt-3">
 <% @categories.each do |category| %>
   <input class="form-check-input" type="radio" name="category_id" value="#{category.id}" id="radio_#{category.id}">
   <label class="form-check-label" for="radio_#{category.id}"><%= category.name %>&emsp;&emsp;</label>
 <% end %>
 </div>

  <br>
  <br>
    <p class="mt-3">・サムネイル画像</p>
  <input id="thumbnail" type="file" name="file" accept="image/*">
  <br>
  <input class="btn btn-primary my-4" type="submit" value="投稿">
  </div>
